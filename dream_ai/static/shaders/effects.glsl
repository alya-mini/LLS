// 50 psychedelic shader presets (seed formulas)
float fx1(vec2 uv, float t) { return sin(1.0*uv.x+t)*cos(2.0*uv.y-t); }
float fx2(vec2 uv, float t) { return sin(2.0*uv.x+t)*cos(3.0*uv.y-t); }
float fx3(vec2 uv, float t) { return sin(3.0*uv.x+t)*cos(4.0*uv.y-t); }
float fx4(vec2 uv, float t) { return sin(4.0*uv.x+t)*cos(5.0*uv.y-t); }
float fx5(vec2 uv, float t) { return sin(5.0*uv.x+t)*cos(6.0*uv.y-t); }
float fx6(vec2 uv, float t) { return sin(6.0*uv.x+t)*cos(7.0*uv.y-t); }
float fx7(vec2 uv, float t) { return sin(7.0*uv.x+t)*cos(8.0*uv.y-t); }
float fx8(vec2 uv, float t) { return sin(8.0*uv.x+t)*cos(9.0*uv.y-t); }
float fx9(vec2 uv, float t) { return sin(9.0*uv.x+t)*cos(10.0*uv.y-t); }
float fx10(vec2 uv, float t) { return sin(10.0*uv.x+t)*cos(11.0*uv.y-t); }
float fx11(vec2 uv, float t) { return sin(11.0*uv.x+t)*cos(12.0*uv.y-t); }
float fx12(vec2 uv, float t) { return sin(12.0*uv.x+t)*cos(13.0*uv.y-t); }
float fx13(vec2 uv, float t) { return sin(13.0*uv.x+t)*cos(14.0*uv.y-t); }
float fx14(vec2 uv, float t) { return sin(14.0*uv.x+t)*cos(15.0*uv.y-t); }
float fx15(vec2 uv, float t) { return sin(15.0*uv.x+t)*cos(16.0*uv.y-t); }
float fx16(vec2 uv, float t) { return sin(16.0*uv.x+t)*cos(17.0*uv.y-t); }
float fx17(vec2 uv, float t) { return sin(17.0*uv.x+t)*cos(18.0*uv.y-t); }
float fx18(vec2 uv, float t) { return sin(18.0*uv.x+t)*cos(19.0*uv.y-t); }
float fx19(vec2 uv, float t) { return sin(19.0*uv.x+t)*cos(20.0*uv.y-t); }
float fx20(vec2 uv, float t) { return sin(20.0*uv.x+t)*cos(21.0*uv.y-t); }
float fx21(vec2 uv, float t) { return sin(21.0*uv.x+t)*cos(22.0*uv.y-t); }
float fx22(vec2 uv, float t) { return sin(22.0*uv.x+t)*cos(23.0*uv.y-t); }
float fx23(vec2 uv, float t) { return sin(23.0*uv.x+t)*cos(24.0*uv.y-t); }
float fx24(vec2 uv, float t) { return sin(24.0*uv.x+t)*cos(25.0*uv.y-t); }
float fx25(vec2 uv, float t) { return sin(25.0*uv.x+t)*cos(26.0*uv.y-t); }
float fx26(vec2 uv, float t) { return sin(26.0*uv.x+t)*cos(27.0*uv.y-t); }
float fx27(vec2 uv, float t) { return sin(27.0*uv.x+t)*cos(28.0*uv.y-t); }
float fx28(vec2 uv, float t) { return sin(28.0*uv.x+t)*cos(29.0*uv.y-t); }
float fx29(vec2 uv, float t) { return sin(29.0*uv.x+t)*cos(30.0*uv.y-t); }
float fx30(vec2 uv, float t) { return sin(30.0*uv.x+t)*cos(31.0*uv.y-t); }
float fx31(vec2 uv, float t) { return sin(31.0*uv.x+t)*cos(32.0*uv.y-t); }
float fx32(vec2 uv, float t) { return sin(32.0*uv.x+t)*cos(33.0*uv.y-t); }
float fx33(vec2 uv, float t) { return sin(33.0*uv.x+t)*cos(34.0*uv.y-t); }
float fx34(vec2 uv, float t) { return sin(34.0*uv.x+t)*cos(35.0*uv.y-t); }
float fx35(vec2 uv, float t) { return sin(35.0*uv.x+t)*cos(36.0*uv.y-t); }
float fx36(vec2 uv, float t) { return sin(36.0*uv.x+t)*cos(37.0*uv.y-t); }
float fx37(vec2 uv, float t) { return sin(37.0*uv.x+t)*cos(38.0*uv.y-t); }
float fx38(vec2 uv, float t) { return sin(38.0*uv.x+t)*cos(39.0*uv.y-t); }
float fx39(vec2 uv, float t) { return sin(39.0*uv.x+t)*cos(40.0*uv.y-t); }
float fx40(vec2 uv, float t) { return sin(40.0*uv.x+t)*cos(41.0*uv.y-t); }
float fx41(vec2 uv, float t) { return sin(41.0*uv.x+t)*cos(42.0*uv.y-t); }
float fx42(vec2 uv, float t) { return sin(42.0*uv.x+t)*cos(43.0*uv.y-t); }
float fx43(vec2 uv, float t) { return sin(43.0*uv.x+t)*cos(44.0*uv.y-t); }
float fx44(vec2 uv, float t) { return sin(44.0*uv.x+t)*cos(45.0*uv.y-t); }
float fx45(vec2 uv, float t) { return sin(45.0*uv.x+t)*cos(46.0*uv.y-t); }
float fx46(vec2 uv, float t) { return sin(46.0*uv.x+t)*cos(47.0*uv.y-t); }
float fx47(vec2 uv, float t) { return sin(47.0*uv.x+t)*cos(48.0*uv.y-t); }
float fx48(vec2 uv, float t) { return sin(48.0*uv.x+t)*cos(49.0*uv.y-t); }
float fx49(vec2 uv, float t) { return sin(49.0*uv.x+t)*cos(50.0*uv.y-t); }
float fx50(vec2 uv, float t) { return sin(50.0*uv.x+t)*cos(51.0*uv.y-t); }
